# macOS (git/.gitignore_global covers every repo; repeated for a fresh clone)
.DS_Store

# Caches that can land here through Stow's folded directories
.zcompdump*

# Secrets — never commit
.env
.env.*

# Private keys — a backstop, since gitzip ships untracked files
id_*
!id_*.pub
*.pem
*.key
